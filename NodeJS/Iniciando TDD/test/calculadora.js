/* 
const app = require("../src/calculadora.js");

describe("Funções de Calculadora", ()=>{

    test("Deve retorna 10 ao somar 5+5",() => {
        let resultado = app.soma(5,5);
        expect(resultado).toEqual(10);
    })

    test("Deve retorna 9 ao multiplicar 3*3",() => {
        let resultado = app.multiplica(3,3);
        expect(resultado).toEqual(9);
    })

})  
*/
